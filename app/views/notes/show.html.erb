<div class="d-flex justify-content-center">
  <% if @note.deleted == 'show' %>
    <%= link_to edit_note_path(@note), class:'underline'  do %>
      <div class="card" style="width: 18rem;  color: black;">
        <%= button_to  note_path(@note), method: :delete, class:'delete-btn' do %>
          <i class="ri-delete-bin-fill"></i>        
        <% end %>
        <%= image_tag @note.image_url if @note.image_url.present? %>
        <div class="card-body">
          <h5 class="card-title"><%= @note.title %></h5>
          <p class="card-text"><%= @note.description %></p>  
          <p class="card-text"><%= @note.date if @note.date.present? %></p>
          <p class="card-text"><%= @note.time  if @note.time.present? %></p>
        </div>
      </div>
    <% end%>
  <% end %>

  <% if @note.deleted == 'bin' %>
      <div class="card" style="width: 18rem;  color: black;">
        <%= button_to  note_path(@note), method: :delete, class:'permanent-delete-btn' do %>
          <i class="ri-delete-bin-fill"></i>
        <% end %>
        <%= button_to  restore_path(@note), method: :post, class:'restore-btn' do %>
          <i class="ri-refresh-line"></i>
        <% end %>
        <%= image_tag @note.image_url if @note.image_url.present? %>
        <div class="card-body">
          <h5 class="card-title"><%= @note.title %></h5>
          <p class="card-text"><%= @note.description %></p>  
          <p class="card-text"><%= @note.date if @note.date.present? %></p>
          <p class="card-text"><%= @note.time  if @note.time.present? %></p>
        </div>
      </div>
  <% end %>

</div> 



<script>
  // deleteBtn = document.querySelector('.delete-btn')
  $('.delete-btn').click( function(){
    alert('Note added to bin')
  })
  $('.permanent-delete-btn').click( function(){
    alert('Note permanent deleted')
  })
  $('.restore-btn').click( function(){
    alert('Note is restored')
  })

</script>
